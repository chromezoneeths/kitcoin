name: Build Edge

on:
  push:
    branches: [edge]

jobs:
  build:
    name: Build images
    runs-on: ubuntu-latest
    steps:
      - name: Check out Git repository
        uses: actions/checkout@v2
      - name: Set up Node.js
        uses: actions/setup-node@v1
        with:
          node-version: 12
      - name: Install Node.js dependencies
        run: npm install
      - name: Compile
        run: gulp ci
      - name: Build
        run: docker build . --file Dockerfile --tag chromezoneeths/kitcoin/kitcoin:edge
      - name: Push
        run: docker push chromezoneeths/kitcoin/kitcoin:edge
